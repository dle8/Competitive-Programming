#include <bits/stdc++.h>
using namespace std;

string s;

bool isPalin(long i, long j) {
	FOR(i, 0, n/2-1) {
		if (s[i]==s[n-])
	}
}

int main(){
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	
	return 0;
}
