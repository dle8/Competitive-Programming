#include <bits/stdc++.h>
#define FOR(i, l, r) for (int i=l; i<=r; i++)
using namespace std;

int main() {
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	char tcase[maxn];
	cin >> n;
	
	count = 0;
	return 0;
}
