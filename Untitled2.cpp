#include <bits/stdc++.h>
#define maxn 100000+10
using namespace std;

int main() {
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	cin >> n >> k;
	FOR(i, 1, n) cin >> a[i];
	return 0;
}
