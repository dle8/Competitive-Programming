#include <bits/stdc++.h>
#define FOR(i, l, r) for (int i=l; i<=r; i++)
#define REP(i, r, l) for (int i=r; i>=l; i--)
#define mod 998244353
using namespace std;

long n, k, fchan[maxn], fle[maxn];

int main() {
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	
	fchan[0] = 0;
	fchan[2] = 
	
	return 0;
}
