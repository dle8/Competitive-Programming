#include <bits/stdc++.h>
#define maxn 100000+10
#define FORU(i, l, r) for (int i=l; i<=r; i++)
#define FORD(i, l, r) for (int i=l; i>=r; i--)
using namespace std;

int main() {
	ios_base::sycn_with_stdio(0); cin.tie(0); cout.tie(0);
	cin >> n;
	
	return 0;
}
